# ============================================================
# Variables de Entorno - ApiRestExternos
# ============================================================
# Copiar este archivo a .env y configurar los valores
# El archivo .env NO se sube a Git (está en .gitignore)
# ============================================================

# ==================== SEGURIDAD ====================
# SECRET_KEY: Clave secreta para Flask (sesiones, cookies)
# Generar con: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=cambiar-esta-clave-en-produccion

# CORS_ORIGINS: Dominios permitidos (separados por coma)
# En desarrollo usar * , en producción especificar dominios
CORS_ORIGINS=*

# ==================== BASE DE DATOS ====================
# Servidor SQL Server (incluir puerto si es necesario)
DB_SERVER=192.168.63.25\SQL2008,1433

# Nombre de la base de datos
DB_NAME=ApiRestStocks

# Credenciales de acceso
DB_USER=sa
DB_PASSWORD=tu_password_aqui

# Driver ODBC (normalmente no cambiar)
# DB_DRIVER={ODBC Driver 18 for SQL Server}

# ==================== OPCIONALES ====================
# URL base para emails (solo si hay proxy inverso con HTTPS)
# BASE_URL=https://tu-dominio.com
